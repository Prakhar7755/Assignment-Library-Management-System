{
  "name": "library-management-system-assignment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn --cwd server install && yarn --cwd client install && yarn --cwd client build",
    "start": "yarn --cwd server start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A Full Stack Library Management System"
}
